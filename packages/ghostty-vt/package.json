{
  "name": "@tui/ghostty-vt",
  "version": "0.1.0",
  "description": "Bun FFI bindings for libghostty-vt terminal emulator",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bash src/wrapper/build.sh",
    "test": "bun test",
    "rebuild:ghostty": "cd ghostty-src && zig build -Demit-lib-vt && cp zig-out/lib/libghostty-vt.dylib ../lib/"
  }
}
